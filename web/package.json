{
  "name": "peach-web",
  "private": true,
  "version": "0.0.0",
  "description": "Peach web for browsing",
  "devDependencies": {
    "bower": "^1.3.1",
    "http-server": "^0.6.1",
    "shelljs": "^0.2.6"
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1"
  },
  "dependencies": {
    "body-parser": "^1.15.0",
    "express": "^4.13.4",
    "fs": "0.0.2",
    "mongoose": "^4.4.7",
    "request": "^2.69.0",
    "util": "^0.10.3"
  }
}
